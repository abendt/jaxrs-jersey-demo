input {
    syslog {
        port => '5000' 
    }
}     

filter {
    json {
        source => 'message'
    }
}

output { 
   elasticsearch { 
        host        => localhost
        protocol    => http 
    } 
}
